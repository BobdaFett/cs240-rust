pub struct Customer {
    customer_number: i32,
    checking_number: i32,
    savings_number: i32
}

impl Customer {
    pub fn new() -> Customer {
        // should this get from the server?
    }

    
}